Öppna både SignalRAngularClient i Visual Studio Code och SignalRApi i Visual Studio el. Code?

* Clenten är byggd i Angular och se till att alla Node compnenter finns med.
* Se till att SignalRApi går att bygga med build i Visual Studio el. Code.

```
När både server och client är igång. Öppna terminalen på det rullande api'et för att se vad som
händer när man klickar på Angular sidan som hittas på localhost:4200
```